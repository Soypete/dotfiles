# keys and stuff
source ~/.secrets

# alias cdmisc="cd ~/code/misc"

export XDG_CONFIG_HOME="$HOME/dotfiles"
export KUBECONFIG=~/kubeconfig

eval "$(~/.local/opt/brew/bin/brew shellenv)"

export SSH_AUTH_SOCK=~/Library/Group\ Containers/2BUA8C4S2C.com.1password/t/agent.sock
export OPENAI_API_KEY="TEST"

source ~/.config/envman/PATH.env

# PATH=$PATH:$HOME/code/google-cloud-sdk/bin/
# PATH=$PATH:$HOME/nvim-macos-arm64/bin/

export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm

# Exo cluster configuration for Claude Code
export EXO_API_URL="http://localhost:52415"
export EXO_LIBP2P_NAMESPACE="soypete-spark-cluster"

# Claude Code with exo backend - use exo-claude function in .zshrc to specify model
# Or set these for default behavior:
# export ANTHROPIC_BASE_URL="${EXO_API_URL}/v1"
# export ANTHROPIC_AUTH_TOKEN=""
# export ANTHROPIC_MODEL="meta-llama/Llama-3.3-70B-Instruct"  # Example model
